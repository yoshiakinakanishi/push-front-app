import * as React from 'react';
import { compose, pure } from 'recompose';

import Layout from '../layouts';

export default compose(pure)(() => (
    <Layout>
        <div>DMM promotion front :)</div>
    </Layout>
));
